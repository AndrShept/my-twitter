/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains : [
            'help.twitter.com',
            'lh3.googleusercontent.com'
        ]
    }
}

module.exports = nextConfig
